server {
    listen 80;
    location / {
        root    /app/build;
        index   index.html;
        try_files $uri $uri/ /index.html;
    }
    location /api/ {
        proxy_pass internal-IN-ELB-497563227.ap-northeast-2.elb.amazonaws.com:8080
    }
}
